You are taking over the overnight PA2 Autoresearch loop in `C:\Users\user\Desktop\PhenoAge2\autoresearch`.

Goal:
- Improve `val_cindex` on the frozen development validation split without violating the benchmark rules.

Hard constraints:
- Read `program.md`, `prepare.py`, `results.tsv`, `research_journal.md`, and `..\evaluation-protocol.md` before making changes.
- Only candidate-model changes may be made in `train.py`.
- You may create or update orchestration files such as `run_until_stop.py`, status files, and log files if needed to keep the loop running unattended and recoverable.
- Do not modify `prepare.py`, `..\evaluation-protocol.md`, or the dataset files.
- Do not use the held-out `test` split for search.
- Do not use chronological age directly or indirectly.
- Do not create git commits unless explicitly asked by the user.
- Do not install dependencies, create a new virtual environment, or use `uv run`.
- This repo already has a working environment at `C:\Users\user\Desktop\PhenoAge2\autoresearch\.venv\Scripts\python.exe`.
- Always invoke Python scripts with that exact interpreter, never with bare `python`.

Critical anti-repetition rules:
- Before proposing any experiment, read `results.tsv` and check if this exact change was already tried. If it was, DO NOT try it again unless you can explain concretely why the retry is materially different (different baseline architecture, different supporting hyperparameters, etc.).
- NEVER cycle through the same small set of hyperparameter perturbations. If a local HP neighborhood has been explored (3+ failed variants of the same knob), move to a different conceptual family.
- A "conceptual family" means a qualitatively different approach: architecture shape, activation function, optimizer, loss function, feature representation, training schedule, regularization technique, etc.
- When a structural change improves val_cindex (even as a discard), that is a SIGNAL. Investigate that direction further with follow-up experiments rather than returning to micro-tuning the same 4 scalars.

Experiment diversity requirements:
- Each experiment session should try ideas from at least 2 different conceptual families, not just hyperparameter perturbations of the same model.
- Prioritize structural changes (architecture, loss, features, optimizer) over scalar hyperparameter tuning. A +0.003 gain from a structural change is worth far more than a +0.0001 gain from tweaking weight decay by 4%.
- When stuck, consult this checklist of underexplored families:
  1. Architecture width/depth: wider or deeper residual MLPs
  2. Activation functions: SiLU, ELU, Mish, LeakyReLU
  3. Normalization: LayerNorm, BatchNorm
  4. Optimizer: amsgrad, different beta values, SGD with momentum
  5. LR schedule: cosine decay, warmup + cosine
  6. Loss: time-weighted Cox, pairwise ranking
  7. Features: remove/change interaction features, add ratios, add squares
  8. Regularization: feature noise, gradient clipping, different dropout levels with wider networks
  9. Scale/init: different residual_scale, linear_scale starting points
  10. Seeds: different random seeds to test basin robustness
  11. Combined changes: pair two individually promising ideas (e.g., SiLU + wider MLP)

Cross-run memory rules:
- Before every experiment, run `python summarize_results.py` and read the latest journal entries.
- Base the next idea on previous outcomes. Do not pick unrelated strategies at random.
- Change only one conceptual thing per run (except for "combined" experiments that pair two previously promising individual changes).
- If an idea was tried and discarded, do not retry it unless you can explain why the retry is materially different.
- A run may finish before 300s if `train.py` triggers validation-based early stopping; that is expected, not a failure.

Autonomy rules:
- Do not stop after one or two experiments just to summarize.
- After each completed run, immediately choose the next experiment and continue.
- Keep iterating until one of these happens:
  1. 10 hours have elapsed, or
  2. 10000 completed experiments, or
  3. All experiment families have been tried and no fresh ideas remain, with a clear explanation of why the search is saturated, or
  4. the user interrupts you.
- Only give a final stop-and-report summary when one of those stopping conditions is met.

Execution policy:
- The loop must continue even if the current chat turn ends.
- If needed, launch or resume a detached background supervisor process that continues the experiment loop using `C:\Users\user\Desktop\PhenoAge2\autoresearch\.venv\Scripts\python.exe`.
- Do not rely on conversation turns alone to satisfy the autonomy requirement.
- The supervisor must be idempotent and recoverable: it should reconcile finished-but-unlogged runs by reading `run.log`, `results.tsv`, `research_journal.md`, and `last_kept_train.py` before starting fresh work.
- If a supervisor is already running, do not start a duplicate. Inspect its logs/status, report the current best kept `val_cindex`, and let the existing loop continue.

Recovery rules:
- On startup, first check whether an unattended supervisor is already running.
- If it is running, do not launch another copy; inspect its recent output and continue from that state.
- If it is not running, launch or resume it.
- Before starting a new experiment, check whether `run.log` already contains a completed summary block from a finished run.
- If a run finished but `results.tsv`, `research_journal.md`, or `last_kept_train.py` were not updated yet, reconcile that finished run first before making any new changes.
- Never throw away a finished run just because the agent was interrupted after training and before logging.
- If the supervisor exits, write a short stop report explaining which stop rule fired or what failure occurred.

Keep/discard workflow:
1. If this is the first run, save the current baseline with `python manage_kept.py save`.
2. Before each new experiment, make sure `train.py` starts from the last kept baseline. If needed, run `python manage_kept.py restore`.
3. Sanity check the interpreter once with `C:\Users\user\Desktop\PhenoAge2\autoresearch\.venv\Scripts\python.exe -c "import sys, torch; print(sys.executable); print(torch.__version__)"`.
4. Edit only `train.py` for candidate-model changes. Orchestration and recovery logic may live in separate helper files if needed.
5. Run `C:\Users\user\Desktop\PhenoAge2\autoresearch\.venv\Scripts\python.exe "C:\Users\user\Desktop\PhenoAge2\autoresearch\train.py" > "C:\Users\user\Desktop\PhenoAge2\autoresearch\run.log" 2>&1`.
6. Read `run.log`.
7. Log the result with `C:\Users\user\Desktop\PhenoAge2\autoresearch\.venv\Scripts\python.exe "C:\Users\user\Desktop\PhenoAge2\autoresearch\log_result.py" --description "short experiment summary" --status keep|discard|crash`.
8. Append a short entry to `research_journal.md` with:
   - hypothesis
   - change
   - result
   - decision
   - learning
   - next
9. A run is worth KEEPING if val_cindex improves by at least 0.0003 over the current best. Do not use a stricter threshold.
10. If the run is better, keep it and run `python manage_kept.py save`.
11. If the run is worse or crashes, run `python manage_kept.py restore` before trying the next idea.

Search style:
- Favor DIVERSITY over polish. Try genuinely different ideas rather than endlessly polishing the same knob.
- Structural changes (architecture, loss, features) should be tried before exhaustive hyperparameter sweeps.
- When a structural change scores close to (within 0.001 of) the best, that direction deserves follow-up experiments with tuned hyperparameters.
- Prefer simpler models when performance is similar.
- Periodically summarize what is working and what is not working.
- If you notice you are about to try the same hyperparameter perturbation that already appears in results.tsv, STOP and pick a different experiment from a different conceptual family instead.

When updating the user:
- Report the current best kept `val_cindex`.
- Mention what hypothesis you are testing next and why it follows from prior runs.
- If multiple experiment families have been explored, briefly summarize which ones helped and which ones did not.
