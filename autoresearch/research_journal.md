## 2026-03-22

- Baseline run used the exact ageless PhenoAge formula from `train.py` with only the 9 original biomarkers and no chronological age.
- Search/evaluation used the full `development` split only and produced `development_cindex = 0.744987`.
- Scripted artifact saved to `C:\Users\user\Desktop\PhenoAge2\autoresearch\candidate_pa2.pt`.
- Medical review of the baseline suggests it already captures three major aging-risk domains without age: inflammation (`CRP`, `WCP`, lower lymphocyte percent), metabolic/renal stress (`SGP`, `CEP`), and frailty/hematologic reserve (`AMP`, `MVPSI`, `RWP`, `APPSI`).
- Hypothesis 1: give extra risk to participants with both elevated CRP and elevated WBC because combined inflammatory activation may matter more than either marker alone. Result: `development_cindex = 0.738183`, worse than baseline, discarded.
- Hypothesis 2: give extra risk to albumin below `40 g/L` because hypoalbuminemia may signal poor physiologic reserve and chronic illness burden beyond the baseline linear term. Result: `development_cindex = 0.737453`, worse than baseline, discarded.
- Restored the kept exact ageless PhenoAge baseline after both simple hypotheses underperformed.
- Hypothesis 3: give extra risk when both RDW is above `14%` and mean cell volume is above `95 fL`, as a simple marker of hematologic frailty that could reflect marrow stress, nutritional deficiency, alcohol-related injury, chronic inflammation, or ineffective erythropoiesis. Result: `development_cindex = 0.745144`, slightly better than the prior baseline `0.744987`, kept because the rule is still easy to explain.
- Hypothesis 4: give extra risk when glucose is above `7.0 mmol/L` and creatinine is above `110 umol/L`, as a simple marker of combined metabolic and kidney stress. Result: `development_cindex = 0.745171`, slightly better than the prior kept score `0.745144`, kept because the rule remains easy to explain to a non-technical reader.
- Hypothesis 5: give extra risk when `CRP` is above `3 mg/L` and lymphocyte percent is below `20%`, as a simple marker of inflammatory stress plus reduced immune reserve. Result: `development_cindex = 0.745035`, slightly worse than the prior kept score `0.745171`, discarded.
- Hypothesis 6: give extra risk when alkaline phosphatase is above `120 U/L` and `CRP` is above `3 mg/L`, as a simple marker of systemic inflammation overlapping with liver/bone-turnover burden. Result: `development_cindex = 0.744906`, worse than the prior kept score `0.745171`, discarded.
- Hypothesis 7: give extra risk when albumin is below `38 g/L` and `RDW` is above `14%`, as a simple marker of poor nutritional reserve plus hematologic frailty. Result: `development_cindex = 0.744789`, worse than the prior kept score `0.745171`, discarded.
- Hypothesis 8: give extra risk when albumin is below `38 g/L` and creatinine is above `110 umol/L`, as a simple marker of low physiologic reserve plus kidney stress. Result: `development_cindex = 0.745275`, slightly better than the prior kept score `0.745171`, kept because the rule is still easy to explain.
- Hypothesis 9: give extra risk when lymphocyte percent is below `20%` and creatinine is above `110 umol/L`, as a simple marker of reduced immune reserve plus kidney stress. Result: `development_cindex = 0.745440`, slightly better than the prior kept score `0.745275`, kept because the rule remains easy to explain.
- Hypothesis 10: simplify the kidney-related reserve logic into one rule that gives extra risk when creatinine is above `110 umol/L` and either albumin is below `38 g/L` or lymphocyte percent is below `20%`. This keeps the idea human-readable as "kidney dysfunction plus low reserve." Result: `development_cindex = 0.745523`, slightly better than the prior kept score `0.745440`, kept.
- Hypothesis 11: simplify the red-cell frailty term into one binary rule that gives extra risk only when both `RDW` is above `14%` and `MCV` is above `95 fL`, instead of adding the amount each exceeds its threshold. Result: `development_cindex = 0.745369`, slightly worse than the prior kept score `0.745523`, discarded.
- Hypothesis 12: make the glucose-plus-kidney term two-tiered so kidney dysfunction with glucose above `7.0 mmol/L` adds one point, and kidney dysfunction with glucose above `10.0 mmol/L` adds an additional point. This was still easy to explain, but the result `development_cindex = 0.745517` was slightly worse than the prior kept score `0.745523`, so it was discarded.
- Hypothesis 13: keep the kidney-plus-low-reserve rule, but add one more point when kidney dysfunction appears alongside both low albumin and low lymphocyte reserve. This stays plain-English as "kidney dysfunction with multiple low-reserve signals is worse than kidney dysfunction with only one." Result: `development_cindex = 0.745541`, slightly better than the prior kept score `0.745523`, kept.
- Hypothesis 14: keep the existing red-cell frailty term, but add one more point when `RDW` is very high above `16%` while `MCV` is also above `95 fL`, as a simple marker of more severe red-cell abnormality. Result: `development_cindex = 0.745540`, essentially tied but slightly worse than the prior kept score `0.745541`, so it was discarded.
- Hypothesis 15: keep the kidney-plus-low-reserve rule, but make the extra severity point depend on low albumin with kidney dysfunction rather than requiring both low albumin and low lymphocytes. This treats albumin as the stronger low-reserve signal while keeping the rule simple to explain. Result: `development_cindex = 0.745574`, slightly better than the prior kept score `0.745541`, kept.
- Hypothesis 16: test the mirror image of the previous idea by making the extra kidney-low-reserve severity point depend on low lymphocyte reserve with kidney dysfunction rather than low albumin. This treats immune reserve as the stronger extra signal. Result: `development_cindex = 0.745731`, better than the prior kept score `0.745574`, kept.
- Hypothesis 17: add one more point when high glucose, high creatinine, and low lymphocyte reserve all co-occur, as a simple metabolic-immune-renal severity signal. Result: `development_cindex = 0.745729`, essentially tied but slightly worse than the prior kept score `0.745731`, so it was discarded.
- Hypothesis 18: add one more point when high glucose, high creatinine, and low albumin all co-occur, as a simple metabolic-renal-low-reserve severity signal. Result: `development_cindex = 0.745731`, exactly tied with the prior kept score `0.745731`, so it was discarded rather than changing the baseline without a gain.
- Hypothesis 19: keep the current kidney-low-reserve structure, but require a stricter lymphocyte threshold below `15%` rather than below `20%` for the extra lymphocyte-priority kidney point. This was easy to explain as focusing on more clearly depleted immune reserve, but the result `development_cindex = 0.745539` was worse than the prior kept score `0.745731`, so it was discarded.
- Hypothesis 20: move the extra lymphocyte-priority kidney threshold in the other direction, so the extra point appears when lymphocyte percent is below `25%` rather than below `20%`. This treats a broader band of low immune reserve as clinically meaningful in the setting of kidney dysfunction. Result: `development_cindex = 0.746029`, better than the prior kept score `0.745731`, kept.
- Hypothesis 21: apply the same broader lymphocyte threshold to the general kidney-low-reserve rule itself, so kidney dysfunction with albumin below `38 g/L` or lymphocyte percent below `25%` gets the low-reserve point, while keeping the extra lymphocyte-priority point at the broader threshold too. Result: `development_cindex = 0.746218`, better than the prior kept score `0.746029`, kept.
- Hypothesis 22: broaden the albumin branch of the kidney-low-reserve rule from below `38 g/L` to below `40 g/L`, to see if mildly low albumin also adds useful reserve information in the kidney-risk subgroup. Result: `development_cindex = 0.746218`, exactly tied with the prior kept score `0.746218`, so it was discarded rather than changing the baseline without a gain.
- Hypothesis 23: keep the broader lymphocyte-based kidney rules, but add one more point when lymphocyte percent falls below `20%` with high creatinine. This keeps the human-readable story that low immune reserve matters in kidney dysfunction, and very low immune reserve matters even more. Result: `development_cindex = 0.746385`, better than the prior kept score `0.746218`, kept.
- Hypothesis 24: mirror the graded immune-reserve idea on the metabolic side by adding one more point when glucose rises above `10 mmol/L` with high creatinine. This stayed simple to explain as "very high glucose plus kidney dysfunction is worse than moderately high glucose plus kidney dysfunction," but the result `development_cindex = 0.746379` was slightly worse than the prior kept score `0.746385`, so it was discarded.
- Hypothesis 25: keep the existing red-cell frailty term, but add one more point when `RDW` is above `14%` and `MCV` is above `100 fL`, as a more specific marker of macrocytic hematologic frailty than the earlier failed red-cell severity rule. Result: `development_cindex = 0.746390`, slightly better than the prior kept score `0.746385`, kept.
- Hypothesis 26: simplify the kidney low-reserve logic by separating it into an albumin path and a lymphocyte path, instead of letting the general kidney-low-reserve rule overlap with the lymphocyte-priority rules. This would have been easier to explain, but the result `development_cindex = 0.746048` was meaningfully worse than the prior kept score `0.746390`, so it was discarded.
- Hypothesis 27: keep the same red-cell severity idea, but lower the added macrocytic cutoff from `100 fL` to `98 fL` while still requiring `RDW` above `14%`. This stayed simple to explain as catching clinically meaningful macrocytosis a bit earlier, but the result `development_cindex = 0.746388` was slightly worse than the prior kept score `0.746390`, so it was discarded.
- Hypothesis 28: keep the same severe macrocytic idea, but require `RDW` above `15%` rather than just above `14%` for the extra point, while still requiring `MCV` above `100 fL`. This stayed easy to explain as reserving the added point for more clearly abnormal anisocytosis plus macrocytosis, but the result `development_cindex = 0.746389` was still slightly worse than the prior kept score `0.746390`, so it was discarded.
- Hypothesis 29: keep the same kidney low-reserve structure, but add one more point when creatinine is high and albumin is more clearly low below `35 g/L`. This stayed easy to explain as "kidney dysfunction with more severe hypoalbuminemia is worse than kidney dysfunction with only mildly low reserve," but the result `development_cindex = 0.746385` was slightly worse than the prior kept score `0.746390`, so it was discarded.
- Hypothesis 30: keep the same broad kidney low-reserve structure, but add one more point when high creatinine co-occurs with both low albumin below `38 g/L` and clearly low lymphocyte reserve below `20%`. This stayed simple to explain as "kidney dysfunction with both nutritional and immune reserve depletion is worse than kidney dysfunction with either signal alone," and the result `development_cindex = 0.746400` was slightly better than the prior kept score `0.746390`, so it was kept.
- Hypothesis 31: after adding the combined severe kidney low-reserve term, simplify the model by removing the extra broad lymphocyte-priority kidney point at lymphocyte percent below `25%` with high creatinine. This would have made the kidney logic easier to explain, but the result `development_cindex = 0.746031` was materially worse than the prior kept score `0.746400`, so it was discarded.
- Hypothesis 32: keep the combined severe kidney low-reserve term, but tighten its albumin branch from below `38 g/L` to below `35 g/L` while still requiring lymphocyte percent below `20%` and high creatinine. This stayed easy to explain as reserving the extra triad point for more clearly severe hypoalbuminemia, but the result `development_cindex = 0.746395` was slightly worse than the prior kept score `0.746400`, so it was discarded.
- Hypothesis 33: leave the kidney-side structure alone, but add one more red-cell severity point when `RDW` is above `16%` and `MCV` is above `95 fL`. This stayed easy to explain as a more clearly abnormal anisocytosis-plus-macrocytosis pattern, but the result `development_cindex = 0.746399` was still slightly worse than the prior kept score `0.746400`, so it was discarded.
- Hypothesis 34: keep the whole current structure, but lower the kidney-related creatinine trigger from above `110 umol/L` to above `100 umol/L` so the added renal-metabolic and low-reserve rules activate slightly earlier. This was easy to explain as testing whether the useful kidney signal begins a bit sooner, but the result `development_cindex = 0.746291` was clearly worse than the prior kept score `0.746400`, so it was discarded.
- Hypothesis 35: keep the current combined kidney low-reserve term unchanged, but tighten the separate severe lymphocyte kidney point from lymphocyte percent below `20%` to below `18%` with high creatinine. This stayed easy to explain as reserving that extra immune-kidney point for more clearly depleted immune reserve, but the result `development_cindex = 0.746286` was clearly worse than the prior kept score `0.746400`, so it was discarded.
- Hypothesis 36: keep the combined kidney low-reserve triad unchanged at lymphocyte percent below `20%`, but broaden the separate severe lymphocyte kidney point from below `20%` to below `22%` with high creatinine. This stayed simple to explain as allowing the extra immune-kidney severity point to activate for a slightly broader but still clearly low lymphocyte range, and the result `development_cindex = 0.746448` was better than the prior kept score `0.746400`, so it was kept.
- Hypothesis 37: rather than changing any biomarker threshold, add one more point when two existing burden domains co-occur: red-cell frailty and kidney low reserve. This is a different kind of hypothesis from the recent threshold-tuning work and stayed simple to explain as "multisystem frailty is worse than isolated frailty." The result `development_cindex = 0.746544` was better than the prior kept score `0.746448`, so it was kept.
- Hypothesis 38: test a different kind of hypothesis again by adding one more point only when three existing burden domains all co-occur: red-cell frailty, renal-metabolic burden, and kidney low reserve. This stayed simple to explain as "three stressed systems together are worse than one or two alone," and the result `development_cindex = 0.746550` was better than the prior kept score `0.746544`, so it was kept.
- Hypothesis 39: test a different kind of hypothesis again by simplifying the recent overlap logic into one burden-count rule instead of separate pairwise and three-way overlap terms. Specifically, add one point when at least two of the three major burden domains are active: red-cell frailty, renal-metabolic burden, and kidney low reserve. This stayed easy to explain as "multiple stressed systems together are worse than a single stressed system," and the result `development_cindex = 0.746558` was better than the prior kept score `0.746550`, so it was kept.
- Hypothesis 40: test a different kind of mathematical hypothesis by leaving all burden rules unchanged but capping their total contribution at `5` points, to see whether diminishing returns and reduced outlier stacking improve ranking. This stayed interpretable as "extra burden helps up to a point, then saturates," but the result `development_cindex = 0.746531` was slightly worse than the prior kept score `0.746558`, so it was discarded.
- Hypothesis 41: keep the same mathematical saturation idea, but use a looser cap of `6` points instead of `5`. This remained simple to explain as preventing extreme stacking without flattening moderate burden combinations too much, and the result `development_cindex = 0.746575` was better than the prior kept score `0.746558`, so it was kept.
- Hypothesis 42: test a different kind of statistical hypothesis by leaving all thresholds and overlap logic unchanged, but capping only the continuous `hematologic_frailty` term at `4` to reduce the leverage of extreme red-cell values. This stayed easy to explain as preventing a few very large RDW/MCV excess values from dominating the ranking, and the result `development_cindex = 0.746622` was better than the prior kept score `0.746575`, so it was kept.
- Hypothesis 43: keep the same statistical robustness idea, but use a looser hematologic cap of `5` instead of `4`. This still fit the same outlier-control story, but the result `development_cindex = 0.746569` was worse than the prior kept score `0.746622`, so it was discarded.
- Hypothesis 44: test a different kind of mathematical/statistical hypothesis by replacing every remaining hard threshold with a short smooth transition curve, using softplus for the excess terms, sigmoids for the binary threshold rules, a smooth activation for the burden-count step, and smooth saturation for the two caps. This stayed interpretable as "risk changes gradually around cutoffs rather than jumping instantly," and the first relatively sharp smoothing preset improved to `development_cindex = 0.747039`, so it was kept.
- Hypothesis 45: keep the full smoothing idea, but broaden the transition zones across all thresholds so the score changes more gradually around each cutoff. This was still easy to explain as reducing threshold sensitivity and local noise, and the result `development_cindex = 0.747717` was better than the prior kept score `0.747039`, so it was kept.
- Hypothesis 46: keep the same fully smoothed structure, but use a very broad set of transition zones across all thresholded rules. This remained simple to explain as treating the clinical cutoffs as rough centers of risk transition rather than precise boundaries, and the result `development_cindex = 0.748358` was better than the prior kept score `0.747717`, so it was kept.
- Hypothesis 47: push the same fully smoothed structure even further with extra-broad transition zones across all thresholded rules. This tested whether the score preferred still more gradual transitions, but the result `development_cindex = 0.747818` was worse than the prior kept score `0.748358`, so it was discarded.
- Hypothesis 48: test a mathematical elegance hypothesis by collapsing the overlapping kidney reserve ladder into one smoothly capped `kidney_reserve_score`, rather than keeping separate kidney low-reserve, lymphocyte-priority, severe lymphocyte, and combined reserve terms. This was easier to read algebraically, but the result `development_cindex = 0.748280` was slightly worse than the prior kept score `0.748358`, so it was discarded.
- Hypothesis 49: test a second mathematical elegance hypothesis by replacing the special `2-or-more domains` overlap trigger with a smooth domain-accumulation curve that grows once more than one major burden domain is active. This was more elegant as a direct count-based curve, but the result `development_cindex = 0.748339` was still slightly worse than the prior kept score `0.748358`, so it was discarded.
- Hypothesis 50: test a third mathematical elegance hypothesis by replacing the separate severe macrocytic step with one smooth macrocytic tail, so the red-cell block rises continuously rather than adding a distinct extra step at very high `MCV`. This was both cleaner mathematically and easier to read, and the result `development_cindex = 0.748535` was better than the prior kept score `0.748358`, so it was kept.
- Hypothesis 51: from a classical-logic perspective, rewrite the kidney block into explicit atomic clauses plus one conjunction bonus, instead of mixing an `OR` low-reserve clause with narrower overlapping subclauses that partially imply one another. This stayed easy to explain as "kidney dysfunction with low albumin and low lymphocytes should be represented by explicit pieces rather than by overlapping logical shortcuts," and the result `development_cindex = 0.748560` was better than the prior kept score `0.748535`, so it was kept.
- Hypothesis 52: from a propositional-logic perspective, partition the kidney lymphocyte ladder into disjoint bands so the "low lymphocyte" bonus covers only the low-but-not-severe region, while the severe clause covers the stronger case. This was logically cleaner because it reduced overlap along an implication chain (`severe` implies `low`), but the result `development_cindex = 0.748542` was slightly worse than the prior kept score `0.748560`, so it was discarded.
- Hypothesis 53: from a first-order-logic perspective, replace the burden-count overlap surrogate with an explicit relation over predicates: "there exist two distinct active burden domains" among hematologic frailty, renal-metabolic burden, and kidney low reserve. This was a different kind of hypothesis from the earlier threshold, smoothing, and overlap-count work, and it stayed easy to explain as "any two stressed systems together should count as a relation, not just as a number." The result `development_cindex = 0.748729` was better than the prior kept score `0.748560`, so it was kept.
- Hypothesis 54: from a higher-order-logic perspective, replace the existential two-domain relation with an aggregation over the whole family of pairwise domain relations. Instead of asking only whether some qualifying pair exists, sum the three pairwise relations among hematologic, renal-metabolic, and kidney-reserve domains and softly cap the result at `2`. This stayed easy to explain as "more co-active system pairs should count more than fewer," and the result `development_cindex = 0.748789` was better than the prior kept score `0.748729`, so it was kept.
- Hypothesis 55: keep the same higher-order pairwise-relation hypothesis, but loosen the cap from `2` to `3` so cases with all three pairwise relations active can contribute a little more. This remained simple to explain as allowing more weight for richer multi-system co-activation, and the result `development_cindex = 0.748795` was better than the prior kept score `0.748789`, so it was kept.
- Hypothesis 56: from an intuitionistic-logic perspective, treat each pairwise domain conjunction as limited by its weakest supporting witness rather than by multiplying the two domain activations. Concretely, replace each pairwise product with `min(left, right)` before aggregating the three pairwise relations. This stayed easy to explain as "a joint burden is only as strong as its weaker side," and the result `development_cindex = 0.748824` was better than the prior kept score `0.748795`, so it was kept.
